list.t.sol
├── when the caller is not the admin
│  └── it should revert
└── when the caller is the admin
   ├── given the address is listed
   │  └── it should do nothing
   └── given the address is not listed
      ├── it should list the address
      └── it should emit a {List} event
