batchCreateWithRange.t.sol
├── when not delegate calling
│  └── it should revert
└── when delegate calling
   ├── when the batch size is zero
   │  └── it should revert
   └── when the batch size is not zero
      ├── it should create a batch of streams with range
      └── it should perform the ERC-20 transfers
