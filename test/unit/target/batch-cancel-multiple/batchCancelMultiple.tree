batchCancelMultiple.t.sol
├── when not delegate called
│  └── it should revert
└── when delegate called
   ├── when the batch size is zero
   │  └── it should revert
   └── when the batch size is not zero
      ├── it should batch cancel multiple
      └── it should perform the ERC-20 transfers
