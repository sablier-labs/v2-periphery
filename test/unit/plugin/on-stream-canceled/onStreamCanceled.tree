onStreamCanceled.t.sol
├── when the call is a standard call
│  └── it should revert
└── when the call is not a standard call
   ├── when the caller is not Sablier
   │  └── it should revert
   └── when the caller is Sablier
      └── it should forward the assets to the proxy owner
