unlist.t.sol
├── when the caller is not the admin
│  └── it should revert
└── when the caller is the admin
   ├── when the address is not listed
   │  └── it should do nothing
   └── when the address is listed
      ├── it should unlist the address
      └── it should emit an {Unlist} event
